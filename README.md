#Huddles restful backend

##Commands
When executing the commands you should be inside the application root directory.

Running locally: `devappserver .`
Testing: `python datastore_test.py {path to gae sdk} .`
